local M = {}

function M.get(c, opts)
	return {
		-- -- buffers
		-- BufferLineBufferSelected = { fg = c.lainoak, bg = c.base03, bold = true },
		-- BufferLineSeparatorSelected = { fg = c.base01 },
		-- BufferLineBackground = { bg = c.base02 },
		-- BufferLineFill = { bg = c.base02 },
		-- --
		-- -- -- (opcional, aunque con slant no afecta mucho)
		-- BufferLineSeparator = { fg = c.focusedBorder, bg = c.base03 },
		--
	}
end

return M
